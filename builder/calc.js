module.exports = function () {
    var calc = {
        add: function (a, b) {
            return a + b;
        },

        sub: function (a, b) {
            return a - b;
        }
    };

    return calc;
};